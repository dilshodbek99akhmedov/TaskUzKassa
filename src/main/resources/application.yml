server:
  port: 8183
spring:
  datasource:
    url: jdbc:h2:file:./db
    username: u
    password: 1
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
nbu:
  currency:
    url: https://nbu.uz/uz/exchange-rates/json/
